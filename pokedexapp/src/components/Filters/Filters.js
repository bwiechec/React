import React from 'react';
import classes from './Filters.module.css';

export default function Filters(props) {
  return (
    <div className={'stat_box ' + classes['filter-box']}>
      test
    </div>
  )
}